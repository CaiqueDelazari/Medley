# Configuração local de ferramenta, não faz parte do site
.claude/

# Prévia do catálogo com dados de mentira — só para conferir o visual
# antes de rodar o SQL. Não entra no repositório e, por isso, também
# não vai para o ar.
site/_previa-catalogo.html

# Lixo do sistema
Thumbs.db
desktop.ini
.DS_Store

# Segredos exclusivos da instalação no VPS
.env
node_modules/
